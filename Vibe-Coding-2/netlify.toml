[build]
  command   = "npm run build"
  publish   = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  timeout      = 120

# SPA fallback — ensure React routing works on direct URL loads
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
